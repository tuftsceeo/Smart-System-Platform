---
format: gfm
execute:
  echo: false
  message: false
  warning: false
editor: visual
editor_options: 
  chunk_output_type: console
---

# Smart System Education Platfrom
```{r setup, include=FALSE}
# Get the absolute file path
file_path <- fs::path_real("README.qmd")  # Adjust as needed for your file path
# Remove the first two folders
path_components <- strsplit(file_path, "/")[[1]]
shortened_path <- fs::path_join(path_components[-c(2, 3)])
```
## Location: `r shortened_path `

### Description
```{r, include=TRUE}
#| child: READMEdescription.md
```

### Directories & Files

The repository has the following directory tree.

```{r}
fs::dir_tree(recurse = FALSE)
```
